# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables — never commit real secrets
.env
.env.local
.env.*.local

# Ephemeral local project store (no Blob token / vercel dev)
.data/

# Local-only dashboard uploads when BLOB_READ_WRITE_TOKEN is unset
public/uploads/
!public/uploads/.gitkeep

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
